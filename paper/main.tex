% @Claude NEVER edit this without explicit instruction to do so
% One sentence per line!
% @claude: make edits in red
% @codex: make edits in green
\documentclass{article} % For LaTeX2e
\usepackage{iclr2026_conference,times}

\usepackage{hyperref}
\usepackage{url}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{dsfont} % \mathds{1} indicator (Type 1 fonts, unlike bbm)
\usepackage{textcomp}
\usepackage{listings}
\lstdefinestyle{promptstyle}{
  basicstyle=\ttfamily\scriptsize,
  breaklines=true,
  breakautoindent=false,
  columns=fullflexible,
  frame=single,
  framesep=4pt,
  xleftmargin=0pt,
  showstringspaces=false,
  keepspaces=true,
}
\usepackage{amsthm}
\newtheorem{proposition}{Proposition}
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

%% Commands for TeXCount
%TC:macro \cite [option:text,text]
%TC:macro \citep [option:text,text]
%TC:macro \citet [option:text,text]
%TC:envir table 0 1
%TC:envir tabular [ignore] word
%TC:envir displaymath 0 word
%TC:envir math 0 word
%TC:envir comment 0 0

%% Red text for drafting notes/highlights: \red{...}
\newcommand{\red}[1]{\textcolor{red}{#1}}
\newcommand{\green}[1]{\textcolor{green}{#1}}

%% Dark blue for the Notes section
\definecolor{darkblue}{RGB}{0,0,139}

%% Indicator function: \ind{d \not\models c_i}
\newcommand{\ind}[1]{\mathds{1}\!\left[#1\right]}

%% Inner product: \inner{q}{d}
\newcommand{\inner}[2]{\left\langle #1, #2 \right\rangle}

\title{Dense Retrieval with }

% Authors must not appear in the submitted version. They should be hidden
% as long as the \iclrfinalcopy macro remains commented out below.
\author{Anonymous Authors \\
Institution \\
\texttt{email@example.com}
}

%\iclrfinalcopy % Uncomment for camera-ready version, but NOT for submission.
\begin{document}

\maketitle

\begin{abstract}
% \red{TODO: abstract.}

% Embedding-based dense retrieval is an important step in many search systems, including e-commerce product retrieval, image search, and others.
As users refine search system queries, they tend to add requirements as conjunctions, making each query more specific than the last.
Prior work has shown that embedding-based retrieval accuracy degrades as the number of conditions grows, motivating methods that remain robust under conjunctive queries.
One limitation in existing training paradigms is the binary training objective; hard negatives that subtly conflict with the query are considered incorrect as easy, obviously irrelevant negatives.
To leverage the wasted information, we present \green{Ours}, a strategy for training embedding systems to handle conjunctive queries.
\green{Ours} combines two strategies to improve models' discrimination: a synthetic data pipeline to infer the number of conditions violated by hard negatives, and a loss function proportional to this count.
We prove that under broad assumptions, our loss is driven to zero by a query vector formed as a superposition of linear probes, one per condition.
In contrast, we show that binary relevance supervision does not confer this structure: embeddings can attain zero binary loss while leaving queried attributes linearly unrecoverable.
\green{Ours} improves \red{baselines}, achieving state-of-the-art performance when combined with \red{InfoNCE(?)}.



\end{abstract}

\section{Introduction}

Embedding-based retrieval aims to recall a match set of semantically relevant documents satisfying the user's query.
Such embeddings are often used in retrieval tasks, such as RAG, product search, and image search, where fine-grained matching of details between the query and the document determine relevance.

Much of this progress is due to broadly applicable techniques, such as instruction tuning and chain of thought.
However, these strategies are not applicable to embedding models.
Combined with the parameter count, representational constraints, and document corpus size on embedding models, this results in a significant language modeling gap between embedding models and generative models; it is common for embedding models to erroneously retrieve documents that a similarly sized generative model can trivially identify as irrelevant.
Thus, we seek a method for distilling the relevant categorization capabilities of a generative model directly into and embedding model.

Specifically, we concentrate on improving conjuntive queries --- those seeking documents that \textbf{have} attributes a, b, c ... and \textbf{do not have} attributes x, y, z, ... . 
This focus is motivated by patterns in user queries and the design of pre-existing non-embedding search systems.
For example, the conjunctive filtering paradigm already exists in product search in the form of attribute filters on e-commerce sites, such as ``screen size $>30$\,cm'' for laptops.
Users find such categories useful both in searching for and understanding products. 
Unfortunately, attribute ontologies are usually fixed, failing when a user desires a product with or without a novel attribute.
Because unique and novel attributes differentiate products, sellers are well motivated to include them in product descriptions to differentiate their products. 
As a result, natural language search systems, such as dense retrieval, are better suited for the long tail of highly specific queries.
Similar use cases exist other retrieval tasks, including for images, videos, resumes, patents, and real estate.

% mechanics - why our system works

Unfortunately, publicly available retrieval datasets, such as \red{cite} mainly contain simple queries, with little to no conjunction or negation.
Instead, datasets like the popular ECSI \red{cite} focus on providing hard negatives---similar pairs of similar documents, only one of which is valid.
This neglects an important dynamic in the user experience of search systems wherein queries tend to gain attributes over time as users refine them. 
\citet{hirsch2020query} find that over 50\% of queries on Ebay occur during a reformulation session, with queries growing in length over time.
Longer queries were associated with increased purchase rates, indicating that support for highly specific queries should be a priority.

There is evidence to believe that queries are underspecified because users doubt the capabilities of recommender systems \citep{kato2014cognitive}.
\citet{papenmeier2020modern} find that the length of users' free-form text descriptions of their desired laptops include on average 29 words and 3.83 attributes. 
However, when Amazon product search, they entered only 2.2 words, indicating that users do not feel including additional attributes would be helpful. 

Users' pessimism is not unfounded, as bi-encoders have known limitations in their expressivity.
% \citet{zhang2025excluir} and \citet{weller2024nevir} show that embedding models struggle to model negation.
These include negation \citep{zhang2025excluir, weller2024nevir}, set operations \citep{killingback2025benchmarking}, constraint dilution \citep{lu2025multiconir}, and attribute binding \citep{yuksekgonul2022and}.
It then follows that user query behavior may suffer from a chicken-and-egg problem; users do not use complex queries because they expect models to fail, while practicioners to do not optimize for complex queries because they do not appear in user data.

To address this gap, we introduce Ours, a strategy for training strong embedding models for conjunctive queries.
We first use an LLM to extract attributes of each document in structured form, creating a large, synthetic dataset with LLM-level accuracy.
We then use a Hamming distance-based loss function to train the embedding model.
This contrasts with baseline loss functions that treat all negatives as equally wrong.
For the query ``warm red hoodie'', the document ``warm blue jacket'' is considered just as incorrect as ``gas-powered lawnmower''.
On the other hand, Ours sets the loss proportional to the squared number of attributes that must be changed to satisfy the query, and much higher if the subject (hoodie vs. lawnmower) must be changed.

\red{Details on results}

\red{Details on theory}

\section{Related Work}

\paragraph{Dense retrieval and graded supervision.}
Dense bi-encoders enable efficient first-stage retrieval by mapping queries and candidates into a shared embedding space~\cite{reimers-gurevych-2019-sentence,karpukhin-etal-2020-dense}.
% Their effectiveness, however, depends strongly on the selection and supervision of negative examples.
Methods such as ANCE and RocketQA improve training by retrieving challenging negatives, sharing negatives across batches, or denoising mined candidates~\cite{xiong2020approximate,qu-etal-2021-rocketqa}.
Other approaches move beyond uniform positive--negative separation by distilling continuous teacher-score differences, as in MarginMSE and TAS-Balanced~\cite{hofstatter2020improving,hofstatter2021tas}, or by using relevance- and attribute-dependent margins in metric-learning objectives~\cite{zhou2020ladder,Jeong_2021_ICCV,Zhao2019}.
\red{todo: discuss ColBERTv2 KL distillation over mined candidates (https://arxiv.org/abs/2112.01488), BGE/C-Pack rank-window ANN mining (https://arxiv.org/abs/2309.07597), NV-Retriever positive-aware negative filtering (https://arxiv.org/abs/2407.15831)}
% These methods demonstrate the value of informative negatives and graded training signals, but generally derive the degree of separation from teacher scores, relevance labels, or broad semantic relationships.
% In contrast, we derive supervision from the discrete structure of the query: candidates are graded by the number of query-relevant feature edits required to make them satisfy its constraints.

\paragraph{Compositional, Boolean, and product retrieval.}
% Product relevance is often compositional, as a relevant item may need to satisfy multiple positive and negative attribute constraints simultaneously.
% Prior product-search methods address this problem through behaviorally informed negative sampling or representations that explicitly model product facets and attribute composition~\cite{Mohan2019,kong2022multiaspect,Choudhary2022}.
Prior product search methods address the problem of compositional requirements through behaviorally informed negative sampling or explicitly representing facets and attribute composition~\cite{Mohan2019,kong2022multiaspect,Choudhary2022}.
A related line of work studies logical composition in dense retrieval.
QUEST considers natural-language queries representing set operations, while BoolQuestions examines conjunction, disjunction, and exclusion through Boolean-aware training and query decomposition~\cite{malaviya-etal-2023-quest,zhang-etal-2024-boolquestions}.
Logical query-embedding methods such as BetaE and ConE instead construct geometric representations that directly support logical operators~\cite{NEURIPS2020_e43739bb,NEURIPS2021_a0160709}.
NevIR shows that bi-encoders are often insensitive to negated meaning, while product-search systems have mitigated related failures through explicit detection and filtering of prohibited attributes~\cite{weller-etal-2024-nevir,Merra2023}.
In the multimodal domain, the CIRR benchmark and methods such as CLIP4CIR study retrieval from queries that combine a reference image with a textual modification~\cite{pmlr-v139-radford21a,Liu_2021_ICCV,Baldrati_2022_CVPR}.
% More recent approaches introduce supervision specifically for negation and fine-grained composition.
CoN-CLIP and NegationCLIP improve sensitivity to negated concepts, while CoLLM uses LLM-generated triplets for composed image retrieval~\cite{Singh_2025_WACV,Park_2025_ICCV,Huynh_2025_CVPR}.

% Negation is particularly challenging for embedding-based retrieval.
% Collectively, these findings expose a limitation of representations dominated by aggregate semantic similarity: candidates may appear highly similar to a query despite violating one or more of its essential constraints.
% Our objective directly supervises this distinction by organizing candidates according to their degree of constraint satisfaction.

\paragraph{Synthetic supervision for retrieval.}
Large language models have increasingly been used to generate targeted supervision for dense retrieval.
Syntriever and SyNeg generate queries, relevant examples, or challenging negatives that expose distinctions absent from conventional training data~\cite{kim-baek-2025-syntriever,li2024syneg}.
\red{todo: discuss LLM-generated negatives in Promptagator (https://arxiv.org/abs/2209.11755), E5-mistral synthetic data (https://arxiv.org/abs/2401.00368), Gecko (https://arxiv.org/abs/2403.20327)}
In product retrieval, Agapaki and Gill combine structured product information with LLM-based relevance assessment to identify difficult training examples~\cite{agapaki2026negative}.
% These approaches show that synthetic data can control which distinctions a retriever encounters during training.
% Our use of generated information differs in an important respect: generated feature annotations specify the candidate structure, but do not directly determine the supervision target.
% Given these annotations and the query constraints, the target distance is computed deterministically as a discrete feature-edit distance.

% \paragraph{Multimodal compositional retrieval.}
% Compositional constraints create analogous difficulties in vision--language retrieval.
% CLIP learns a shared embedding space for images and text, whereas the CIRR benchmark and methods such as CLIP4CIR study retrieval from queries that combine a reference image with a textual modification~\cite{pmlr-v139-radford21a,Liu_2021_ICCV,Baldrati_2022_CVPR}.
% More recent approaches introduce supervision specifically for negation and fine-grained composition.
% CoN-CLIP and NegationCLIP improve sensitivity to negated concepts, while CoLLM uses LLM-generated triplets for composed image retrieval~\cite{Singh_2025_WACV,Park_2025_ICCV,Huynh_2025_CVPR}.
% This literature further demonstrates that general semantic alignment does not reliably encode whether each positive and negative constraint is satisfied.

% \paragraph{Positioning of our work.}
% Prior research has improved compositional retrieval through hard-negative mining, teacher-derived graded supervision, Boolean-aware modeling, attribute-sensitive representations, and synthetic example generation.
% Our work connects these directions by introducing a discrete, query-relative structural distance: the minimum number of query-relevant feature edits required for a candidate to satisfy the query.
% Unlike teacher scores or learned relevance estimates, this distance is deterministic once the query constraints and candidate features are specified.
% We use it as graded supervision for first-stage retrieval, encouraging distances in the learned embedding space to reflect degree of constraint satisfaction.
% We evaluate the resulting formulation in both textual and image-based product retrieval.






\section{Evaluation and Dataset Construction}
We seek to evaluate models on a test dataset containing query-document pairs where the document satisfies all constraints in the query.
At the same time, we want the training dataset to contain hard negative examples for those queries that do not satisfy all their constraints.
Due to the lack of structured attributes in existing datasets, we construct a synthetic data pipeline based on the observation that current generative LLMs can extract the necessary attributes.
In effect, this pipeline seeks to distill fine-grained attribute extraction from large, generative models into small embedding models.

We target two domains, e-commerce products and images, to demonstrate the multimodal capabilities of our system.
For e-commerce, we choose the ESCI corpus of Amazon products \red{cite} due to its large size.
For images, we choose the DeepFashion In-Shop because it contains images with subtle but objectively differing attributes.

To extract document attributes, we prompt Gemini 2.5 Flash with a matched positive and negative document pair, using the prompt of Appendix~\ref{prompt:text-features} for products and Appendix~\ref{prompt:image-features} for images.
We select candidates for hard negatives in ESCI using the pre-existing Substitute and Irrelevant labels.
For DeepFashion, we select a random example from the same category, such as ``blouses'' or ``graphic tees''.
In the DeepFashion dataset, we include images of both products in the prompt, instructing the model not to generate attributes that are not visible in the images.

We instruct it to generate up to 5 attributes that apply to only the first example, only the second example, to both examples, or to neither example, a pool of up to 20 attributes.
We then select a random subset of these attributes to compose the query, negating attributes that do not apply to the positive example, ensuring that at least one attribute is not satisfied by the hard negative example.
Oversampling here is useful to ensure that selected attributes are not merely the most salient.
Thus, we obtain a set of $k$ attributes, all of which are satisfied by the positive example and $k'$ of which are \emph{not} satisfied by the hard negative example, where $1 \leq k' \leq \min(k, 10)$.

We construct a proto-query with the template "I am looking for: {item} that has: \{positive\} + \{common\} attributes; and does not have: \{negative\} + \{neither\} attributes".
We then prompt Gemini 2.5 Flash to rewrite the proto-query based on five human-written in-context examples, replacing attribute phrases with synonyms (Appendix~\ref{prompt:rephrase}); the prompt includes the positive document so synonyms stay consistent with it, requires at least half of the attributes to be reworded, and forbids changing numeric values.
% rephrasing and non-exact match, prompting the model to preserve intent and numeric values while replacing most attribute phrases with synonyms (Appendix~\ref{prompt:rephrase}).
Rewriting the query in this way is designed to improve the realism and diversity of queries and reduce surface form overlap between queries and positive documents.
This yields a (query, positive, hard negative) triple.
We also sample an easy negative at random from the same split.
The easy negative is drawn from a different category than the positive for DeepFashion.
This yields a matching (query, positive, easy negative) triple.
We divide this dataset into train, validation, and test splits, ensuring that no query or document appears in multiple.
Note that the hard negative mining procedure can be applied to virtually any dataset or modality that can on which an LLM can perform binary QA, thus we consider it the first contribution of this paper.


We manually annotate \red{xx} attributes across \red{xx} products in each dataset and find that the attributes are correct \red{xx\%} and \red{xx\%} of the time in ECSI and DeepFashion, respectively.


\section{Baselines}

Numerous training objectives have been proposed for dense retrieval.
We choose MSE, CoSENT, SigLIP, and InfoNCE \red{cite} as our baseline loss functions based on their applicability to triple-supervised training and coverage of high level loss strategy families. 
MSE regresses query-document pairs onto a binary target; CoSENT ranks pairs ordinally; SigLIP classifies in-batch pairs independently; InfoNCE scores each in-batch candidate against the others using a softmax function.
We include their definitions in Appendix \ref{app:loss-functions} for reference.
Because batch size affects CoSENT, SigLIP, and InfoNCE training dynamics through pairwise in-batch comparisons, we hold batch size constant across all runs.
To do so, we expand the batch to include $B$ positives and $B$ negatives, comparing all positives to all positives and all negatives, for a total of $2B^2$ comparisons, as opposed to $B^2$, conventionally.
This permits imcorporating the baseline NV-Retriever and our proposed hard negative mining algorithms without confounding comparisons per batch \red{(Section \ref{??})}.
We design our experiments such that each model sees each training triple once per epoch.
We train every model for one epoch with 32 rows per forward pass and 4 forward passes per optimizer step.
The pair-based losses (MSE, CoSENT) emit two pairs per training triple, so their batches contain about half as many positives as the triple-based losses.
We train baseline models using hyperparameters listed in \red{Appendix \ref{??}}, concluding that InfoNCE is the strongest (Table \ref{tab:baseline-loss}).
% \red{To further strengthen our baseline, we adopt the positive-aware hard negative mining of NV-Retriever \citep{moreira2024nv}, which the survey of \citet{wischounig2026negative} classifies under false-negative filtering and which underlies the top retrieval model on the MTEB leaderboard in their Table 5 \textcolor{orange}{(cite NV-Embed-v2)}, and apply it to InfoNCE.}
To further strengthen our baseline, we apply the positive-aware hard negative mining algorithm NV-Retriever, a key component of NV-Embed-v2 \citep{lee2024nv}, the strongest model based on hard negative mining listed in \citet{wischounig2026negative}.
We choose the best of $n=8$ hyperparameter combinations based on a grid search \red{(Appendix \ref{??})}.


\begin{table}[t]
\centering
\input{figs/baseline_loss_table.tex}
\caption{Validation split recall of baseline loss functions trained on binary labels with randomly selected negatives, $n=3$. +NV indicates the model was trained using negatives mined with NV-Retriever. \red{delete?}}
\label{tab:baseline-loss}
\end{table}

\subsection{Model Details}

We fine-tune \texttt{all-mpnet-base-v2} on text and \texttt{clip-ViT-B-32} on images, both as distributed by Sentence-Transformers \red{(cite)}; the text tower encodes queries in both modalities, and we normalise every representation to unit length.
We optimise with AdamW \red{(cite)} at learning rate $2 \times 10^{-5}$, a cosine schedule warming up over the first 10\% of steps, and bf16, training each condition three times and report the mean over those trials.


\section{Methods}
\label{sec:methods}

Traditional Margin-MSE loss \citep{hofstatter2020improving} using inner product similarity is defined as

% \begin{equation}
%     \mathcal{L}{q, x^+, x^-} = \Big( \inner{f(q)}{f(x^+)} - \inner{f(q)}{f(x^-)} \Big)^2
% \end{equation}
\begin{equation}
    \label{eq:trad-mse}
    \mathcal{L}(q, x^+, x^-) = \Big( \inner{f(q)}{f(x^+) - f(x^-)} - y \Big)^2 .
\end{equation}

Where $q$ is a query, $x^+$ is a relevant document, $x^-$ is a less relevant document, and $y$ is a scalar representing the margin between their relevance as determined by a teacher model.
$f$ is a function mapping queries or documents to their embeddings. 
Throughout this paper we take $\inner{\cdot}{\cdot}$ to be cosine similarity.



Formally, following \citep{lu2025multiconir}, let query $q_k$ be a query composed of $k$ conditions $C=\{c_1, c_2, ..., c_k\}$ with $k \in \{1, ..., \red{K}\}$.
We say a document $x$ is a positive document, $x^+$, with respect to $q_k$ if $x^+ \models C$, i.e., $x^+$ satisfies all $k$ conditions.
Otherwise, we say the document $x^-$ is negative with respect to $q_k$.




\subsection{Attribute Hamming Loss}



% We introduce Attribute Hamming Loss, which is a form of Margin-MSE loss designed to distil knowledge from generative models rather than teacher embedding models.
We introduce Attribute Hamming Loss, a strategy for assigning partial credit to hard negative examples.
The idea behind Attribute Hamming Loss is to modify a loss function such that the margin between a query and a document is proportional to the number of constraints violated by a document.
Formally, we define Attribute Hamming Distance $d$ as
% Attribute Hamming Loss replaces the target $y$ with an integer representing the number of constraints in the query violated by $x^-$, rescaled by a constant $V$.
% Training operates on triples $(q_k, x^+, x^-)$ in which the positive satisfies every condition of the query, and the predicted margin between the positive and negative similarities is regressed onto the number of query constraints the negative violates:

\begin{equation}
    d(x, q) =
    % \begin{cases}
        % \eta & \text{if } x \text{ is an easy negative} \\
        \sum_{i=1}^{k} \ind{x \not\models c_i} %& \text{otherwise}
    % \end{cases}
\end{equation}

We then modify modify each loss function, with the modification marked by an underbrace:

\paragraph{Margin-MSE:}
We set the margin term to $d/V$:

\begin{equation}
\label{eq:hamming}
    \mathcal{L_\text{MSE}}(q_k, x^+, x^-) = \Big( \inner{f(q_k)}{f(x^+) - f(x^-)} - \underbrace{\tfrac{d}{V}}_{\text{ours}} \Big)^2
\end{equation}

Where the violation scale hyperparameter $V$ controls the penalty per violation.
Let $K^*$ be the maximum number of constraints violated by any example.
In Margin-MSE only, we set $d(x,q)=\eta \geq K^*$ in the case when x is an easy negative.
Choosing $V \geq \text{max}(\eta, K^*)$ ensures the $d/V$ term is in $[0, 1]$.

\paragraph{InfoNCE:}
Every document in a batch of $B$ triples is a candidate for every query: the $B$ positives and the $B$ hard negatives, the set $X$ of $2B$ candidates. InfoNCE trains the softmax over $X$ toward a one-hot target on the query's own positive. We keep the logits and change the target: the own positive keeps mass $1$, the own hard negative receives mass $e^{-s\,d/V}$ for its measured $d$, and every candidate drawn from another row, positive or negative, is a random product with respect to $q_i$ and receives no mass. Rows are normalized to sum to one:

\begin{align}
    \label{eq:ours-infonce-target}
    T_i(x) &=
    \begin{cases}
        1 & x = x_i^+ \\
        e^{-s\, d(x_i^-, q_i) / V} & x = x_i^- \\
        0 & \text{otherwise,}
    \end{cases} \\[0.5em]
    \label{eq:ours-infonce}
    \mathcal{L}_\text{INCE}(q_i) &= - \sum_{x \in X} \underbrace{\frac{T_i(x)}{\sum_{x' \in X} T_i(x')}}_{\text{ours}} \log \frac{\exp\big( s \inner{f(q_i)}{f(x)} \big)}{\sum_{x' \in X} \exp\big( s \inner{f(q_i)}{f(x')} \big)} .
\end{align}

As $V \rightarrow 0$ this becomes equivalent to the original InfoNCE loss.

% Earlier variant (ours-infonce-margin), superseded by the target-mass form above on 2026-09-03:
% margins d/V added to each candidate's cosine inside the softmax, target one-hot.
%   L(q_i) = - log exp(s cos(q_i, x_i^+)) / sum_j [ exp(s (cos(q_i, x_j^+) + d(x_j^+, q_i)/V)) + exp(s (cos(q_i, x_j^-) + d(x_j^-, q_i)/V)) ]
% with d = 0 on the own positive, the measured d on the own hard negative, and eta on every cross-row candidate.

\paragraph{SigLIP:}
In place of the original binary target, we weight each pair with $T_i(x)$:

\begin{equation}
    \label{eq:ours-siglip}
    \mathcal{L}_\text{SigLIP} = - \frac{1}{B} \sum_{i=1}^{B} \sum_{x \in X} \left[ \underbrace{T_i(x)}_{\text{ours}} \log \frac{1}{1 + e^{-\left( s \inner{f(q_i)}{f(x)} + b \right)}} + \underbrace{\big( 1 - T_i(x) \big)}_{\text{ours}} \log \frac{1}{1 + e^{s \inner{f(q_i)}{f(x)} + b}} \right] ,
\end{equation}

The unifying factor among these modifications is to regress towards the condition 

\begin{equation}
    \inner{f(q)}{f(x_1)} - \inner{f(q)}{f(x_2)} = (d(q, x_2) - d(q, x_1))/V
\end{equation}

% Earlier variant (ours-siglip), superseded by the target form above on 2026-09-14: the linear
% target T_i(x) = 1 - d(x, q_i)/V, i.e. 1 for the own positive, 1 - d/V for the own hard negative,
% and 1 - eta/V for every cross-row candidate.
% Implementation detail, cut 2026-09-15 to keep the paragraph to the ours/traditional difference:
% the two labeled cells of each row and the 2B - 2 cross-row cells are averaged separately and
% mixed with equal weight, for both the graded loss and its binary control, so that the per-row
% signal is not outvoted by the cross-row cells.

\paragraph{CoSENT:}
We assign rank 1 to positives, 1/2 to hard negatives, and 0 to easy negatives. 
The algorithm otherwise remains unchanged.
% \red{CoSENT reads its labels only through their ordering, so we assign each pair $(q_i, x_i)$ one of three ranks:}

% \begin{equation}
%     \label{eq:ours-cosent-rank}
%     \red{\boldsymbol{r_i}} =
%     \begin{cases}
%         \red{\boldsymbol{1}} & \text{if } x_i \text{ is the positive} \\
%         \red{\boldsymbol{\tfrac{1}{2}}} & \text{if } x_i \text{ is a hard negative} \\
%         \red{\boldsymbol{0}} & \text{if } x_i \text{ is an easy negative,}
%     \end{cases}
% \end{equation}

% and the loss is unchanged:

% \begin{equation}
%     \label{eq:ours-cosent}
%     \mathcal{L}_\text{CoSENT} = \log \Big( 1 + \sum_{r_i > r_j} \exp\big( s \big( \inner{f(q_j)}{f(x_j)} - \inner{f(q_i)}{f(x_i)} \big) \big) \Big) ,
% \end{equation}

% \red{The distance $d$ does not enter: a hard negative violating one constraint and one violating ten are ranked equally, above every easy negative and below the positive.}


% In Section \ref{???} we describe a data synthesis pipeline to construct training triples with known target values of $\sum_{i=1}^{k} \ind{x^- \not\models c_i}$.
% The target margin is zero when the document satisfies every constraint (positive) and grows by $1/V$ per violated constraint (hard negative).
% This contrasts with binary classification training regimes, such as traditional triplet loss, where examples are either strictly correct or incorrect.

% One can imagine this loss function attempting to approximate bit-wise filtering on a table of $n$ documents and arbitrarily many columns of attributes. 
% Because for any given query $q_k$, only the $k$ attributes present in the query are relevant, the space requirement of this dataset can be bounded by $\mathcal{O}(nk)$.

...

\subsection{Relation to Linear Probing}

% For a given $c_i$, 
Following \citet{garg2026many}, we will assume that inputs come from some set $L = Q \cup D$ being queries or documents composed of sequences of tokens.
Let $f : L \rightarrow \mathbb{R}^d$ give the embeddings of dimension $d$.
We let $z_i : L \rightarrow \{0,1\}$ be an attribute of a query or corresponding product, for example, whether a query requires a red product, or whether the product in question is red. 
Note that the value of $z_i$ only depends on the text.


% Under the Linear Representation Hypothesis, we assume that for a set of features $z_1, ..., z_m : L \rightarrow \mathbb{R}$

Under the Linear Representation Hypothesis, we say a feature is $(\epsilon, S)-$ linearly recovered for some linear probe vector $b_i \in \mathbb{R}^d$ if 
% Let $b_i \in \mathbb{R}^d$ be a probe vector for attribute $z_i$ such that

\begin{equation}
    |\inner{b_i}{f(\ell)} - z_i(\ell)| < \epsilon 
\end{equation}

for all $\ell \in S$

\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{figs/embedding_fig.pdf}
    % \caption{An idealized embedding space in $\mathcal{R}^4$, projected into $\mathcal{R}^3$. Three documents, each with exactly one of the attributes red, cotton, and zipper, embed embed.; because each has a single attribute, each direction is also an exact linear probe for its attribute. The query requiring red and zipper embeds as the normalized superposition of its two probes, so its cosine similarity with a document is proportional to the number of queried attributes the document satisfies: $1/\sqrt{2}$ for $x_{\mathrm{red}}$ and $x_{\mathrm{zipper}}$, $0$ for $x_{\mathrm{cotton}}$. The proportionality constant is $1/\sqrt{2}$ here only because the documents span the whole space; Theorem~\ref{thm:forward} fixes it at $1/V$ for every query by padding the query with a component orthogonal to the document span, which a three-dimensional picture has no room to show.}
    \caption{An idealized embedding space in $\mathbb{R}^4$, drawn by dropping the fourth coordinate. Three documents, each with exactly one attribute, have mutually orthogonal visible parts and put the rest of their unit length into a fourth component the query cannot see. The query is the normalized sum of its attributes' linear probes $b_i$. With $V = 3/\sqrt{2}$ as the violation scale, the probe sum has norm $\|b_{\mathrm{red}} + b_{\mathrm{zipper}}\| = V$, and $V \inner{f(q)}{f(x)}$ counts the conditions $x$ satisfies: $x_{\mathrm{red}}$ and $x_{\mathrm{zipper}}$ score $1/V = \sqrt{2}/3$, $x_{\mathrm{cotton}}$ scores $0$, and the positive $x_{\mathrm{red,zipper}}$, which the probes force to be the visible sum of $x_{\mathrm{red}}$ and $x_{\mathrm{zipper}}$, scores $2/V$. Any larger $V$ is absorbed by the document embeddings moving more of their length into the fourth component the query cannot see.}
    \label{fig:embedding}
\end{figure}


\section{Experiments}



% Both datasets are built by the same two-stage procedure: pair a positive document with a hard negative, then use a language model to name the attributes that separate them.
% The text dataset is derived from the ESCI shopping-queries corpus, restricted to the US locale and to products whose descriptions are detected as English.
% For each query we take an \texttt{Exact} product as the positive $x^+$ and a \texttt{Substitute} or \texttt{Irrelevant} product for the same query as the hard negative $x^-$, discarding queries with no \texttt{Exact} match; an easy negative is drawn uniformly from the corpus.
% The image dataset is built from DeepFashion In-Shop (\texttt{Marqo/deepfashion-inshop}), grouping views of the same SKU by product identifier.
% The positive and hard negative are distinct products sharing a category (\texttt{category2} by default, optionally also matched on colour), and the easy negative is drawn from a different category.
% Both pipelines are seeded at $42$ and prompt \texttt{gemini-2.5-flash}, the image pipeline passing both photographs to the model alongside product metadata.
% The model returns four disjoint attribute buckets of at most $N=5$ entries each: features common to both products, features unique to $x^+$, features unique to $x^-$, and features applying to neither.
% Attributes are constrained to be objective, at most five words, and stated positively, so that negation enters only through the query template rather than through the attribute strings.

% Queries are then assembled from these buckets.
% We draw a target distance $k \sim \mathrm{Unif}\{1, \dots, 10\}$ and sample $n_{\text{pos}} \sim \mathrm{Unif}\{0, \dots, k\}$ unique-positive attributes, $n_{\text{neg}} = k - n_{\text{pos}}$ unique-negative attributes, and independently $\mathrm{Unif}\{0, \dots, k\}$ attributes from each of the common and neither buckets, every count capped by the size of its bucket.
% At least one attribute is forced from the positive-or-common side and one from the negative-or-neither side, so every query carries both a requirement and a prohibition and $|C| \ge 2$.
% The natural-language query is rendered as \emph{I am looking for: ``\{item\}'' that has: \{positive and common attributes\}; and does not have: \{negative and neither attributes\}}, placing unique-positive and common attributes under the positive polarity $s_i = +1$ and unique-negative and neither attributes under $s_i = -1$.
% The regression label is $n_{\text{pos}} + n_{\text{neg}}$, counting only the attributes that distinguish $x^+$ from $x^-$; common and neither attributes are satisfied identically by both documents and contribute nothing.
% Each pair yields two training triples, one against the hard negative and one against the easy negative, the latter carrying a sentinel label remapped at training time to $15$.
% Labels are divided by $V = 25$ and used without further shaping, the linear setting of the transform.
% The image split contains $20{,}000$ triplets.

\begin{table}[t]
\centering
\label{tab:datasets}
\vspace{1em}
\small
\begin{tabular}{lrrrr}
    \toprule
    & \multicolumn{2}{c}{\red{Text}} & \multicolumn{2}{c}{\red{Image}} \\
    \cmidrule(lr){2-3} \cmidrule(lr){4-5}
    & \red{Synthetic} & \red{Human} & \red{Synthetic} & \red{Human} \\
    \midrule
    % \red{Domain} & \multicolumn{2}{c}{\red{Amazon product descriptions}} & \multicolumn{2}{c}{\red{Clothing images}} \\
    Corpus documents               & \red{144{,}859} & \red{131} & \red{27{,}431} & \red{55} \\
    Training documents             & \red{123{,}416} & \red{---} & \red{23{,}379} & \red{---} \\
    Validation documents           & \red{10{,}419}  & \red{---} & \red{3{,}776}  & \red{---} \\
    Test documents                 & \red{11{,}061}  & \red{131} & \red{3{,}765}  & \red{55} \\
    Words per document             & \red{186.4}     & \red{186.6} & \red{---} & \red{---} \\
    Words per query                & \red{37.5}      & \red{11.4} & \red{34.0}  & \red{9.4} \\
    \red{Attributes stated per query}          & \red{9.9} & \red{2.9} & \red{9.8} & \red{3.1} \\
    \red{Differentiating attributes per query} & \red{5.1} & \red{---} & \red{5.1} & \red{---} \\
    \bottomrule
\end{tabular}
\caption{Dataset summary statistics. Document counts for Synthetic include hard negative pairs while Human documents are positive-only.}
\end{table}



\subsection{Ours Details}

For each modified loss function, we perform a hyperparameter sweep on the validation set across $V \in \{10, 20, 40, 80\}$, and also $\eta \in \{10, 20, 40\}$ for Margin-MSE. 
We perform ablations that only use our dataset creation process without modifying baseline loss functions.
\red{Finally, we explore mixing data 50-50 from our dataset creation and hard negatives mined with NV-Retriever, based on \cite{moreira2024nv}'s analysis that NV-Retriever improves training stability while our method trains discrimination of fine-grained conjunctive attributes.}

...

\section{Results}

We present results in Figure \ref{fig:text-vs-image}.
On the larger synthetic test set, we see significant improvement compared to baselines applying both attribute-aware mining and attribute Hamming loss.
Overall accuracy is stratified by loss function, with InfoNCE ranking highest on both modalities, both for our proposed methods and baselines.
Both baseline NV-Retriever and attribute-aware mining significantly increase recall on text, but improvement on images is mixed and modality dependent.
Attribute-aware mining is more effective than NV-Retriever on both modalities using the two strongest loss functions, InfoNCE and SigLIP.
After applying attribute Hamming loss, however, our method surpasses all baselines using all loss functions in all modalities.
One exception is InfoNCE Image, where our method ties the unmined baseline, possibly due to saturation.
\red{Finally, when trained on a mix of examples mined by NV-Retriever and attribute aware mining, we see improvement on CoSENT Image and a slight improvement on both InfoNCE modalities, though check statistical significance.}


\red{main results, we beat baselines}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figs/text_vs_image_recall.pdf}
    \caption{Text Recall@5 vs. image Recall@20 on the test split ($n=3$ per point). Arrow direction indicate that one training strategy builds on another. Unfilled shapes indicate baselines.}
    \label{fig:text-vs-image}
\end{figure}

\red{more positives = easier, more negatives = no relation}




\section{Notes}
\begingroup\color{darkblue}

\red{Working notes --- not for submission.}

We characterize the optimum of the attribute Hamming objective.
For a training triple the label equals a signed difference of attribute values, $y = \sum_{i \in C} s_i ( z_i(x^+) - z_i(x^-) )$, so a query representation formed as the signed sum of per-attribute probes matches the regression target termwise.
The correspondence holds in both directions: if every queried attribute is linearly recoverable from the document embeddings at error $\epsilon$, the query vector $\sum_{i \in C} s_i b_i$ attains loss below $\red{(2k\epsilon/V)^2}$ (Theorem~\ref{thm:forward}); conversely, if the query family is closed under deleting a condition, a model with loss below $\delta^2$ on every triple yields a probe recovering each queried attribute at error $\red{2V\delta}$ (Corollary~\ref{cor:delete}).
Under these hypotheses, minimizing the objective and making the query embedding a simultaneous linear probe for the attributes it names are the same requirement.
Triplet and in-batch objectives constrain only the ordering of scores and are satisfied by any embedding that ranks the positive first by at least the margin; constraining the margin's magnitude to a count makes partial constraint satisfaction a graded, linearly decodable quantity.
Under the additional assumption that document embeddings satisfy the linear representation hypothesis with $k'$-sparse attribute vectors, the construction of \citet{garg2026many} realizes loss $\delta^2$ at dimension $d = O(k'^2 k^2 \delta^{-2} \log m)$ (Corollary~\ref{cor:capacity}).
The dependence on the number of attributes is logarithmic, so a fixed-width embedding supports exponentially many.
% Throughout, the objective is the dot-product form of Definition~\ref{def:obj}; normalization enters only through the label scale (Remark~\ref{rem:scale}).
% These are statements of capacity, not optimization: they establish that representations with this structure exist and that the objective's optimum has it.

% \paragraph{Motivation.}
% The optimum of our objective is a query embedding that acts as a simultaneous linear probe for the
% attributes the query names, in the sense of \citet{garg2026many}, with the two directions agreeing
% up to a factor $2k$ in the error.
% Three things follow.
% \begin{enumerate}
%     \item \emph{A capacity statement for attribute-conditioned retrieval.}
%     \citet{garg2026many} answer how many features a $d$-dimensional representation can store and
%     linearly decode. That question is about language models, and on its own says nothing about
%     retrieval. Corollary~\ref{cor:capacity} supplies the bridge. Assuming document embeddings are
%     linearly represented with $k'$-sparse attribute vectors, loss $\delta^2$ on $k$-condition
%     queries is realizable at $d = O(k'^2 k^2 \delta^{-2} \log m)$, polynomial in the sparsities and
%     logarithmic in the attribute vocabulary, so exponentially many attributes fit at fixed width.
%     This is the formal version of the claim that the open-vocabulary long tail
%     described in the introduction is reachable at all.
%     \item \emph{A mechanism for why graded supervision helps.}
%     Triplet and in-batch objectives, on the other hand, constrain only the \emph{ordering} of scores, and are satisfied
%     by any embedding that ranks the positive first. Ours constrains the margin to equal a
%     \emph{count}, and Theorem~\ref{thm:forward} shows this is what forces each queried attribute onto
%     a linear direction. This is the precise content of the claim that we distill categorization structure into
%     the embedding.
%     \item \emph{A prediction we can measure.}
%     Theorem~\ref{thm:back} and Corollary~\ref{cor:delete} say a model with loss below $\delta^2$
%     on every triple admits probes of error $O(\delta)$. Fitting
%     linear probes post hoc on our checkpoints therefore tests the theory directly, and predicts that
%     baselines trained to comparable loss yield worse probes.
%     \red{Run this against the attribute labels in \texttt{analysis/}.}
% \end{enumerate}
% The scope is capacity, not optimization: the bounds certify that suitable embeddings and query
% representations exist at a given dimension, not that training finds them.


% The following is a self-contained proof that minimizing the attribute Hamming objective is equivalent, up to a factor $2k$ in the error, to requiring the query embedding to act as a simultaneous linear probe for the attributes its conditions name, in the sense of \citet{garg2026many}.
% We first fix notation and record four elementary lemmas, then prove the two directions.

\subsection{Setup}

\begin{definition}[Attributes and conditions]
\label{def:attr}
Let $L$ be the set of texts, comprising both queries and documents, and let $z_i : L \rightarrow \{0,1\}$ for $i \in [m]$ be attribute indicators, with $z_i(x) = 1$ when $x$ has attribute $i$.
A \emph{condition} $c_i$ names an attribute $i$ together with a \emph{polarity} $s_i \in \{-1, +1\}$.
A document $x$ \emph{satisfies} $c_i$, written $x \models c_i$, when $z_i(x) = 1$ in the case $s_i = +1$, and when $z_i(x) = 0$ in the case $s_i = -1$.
A query $q_C$ carries a set of conditions $C$ with $|C| = k$, and $x \models C$ means $x \models c_i$ for every $i \in C$.
\end{definition}

Note that $s_i^2 = |s_i| = 1$, used repeatedly below.

\begin{definition}[Embedding and objective]
\label{def:obj}
Let $f : L \rightarrow \mathbb{R}^d$ assign a \red{unit-norm} representation\red{, $\|f(\ell)\| = 1$,} to each text\red{, so that $\inner{\cdot}{\cdot}$ is cosine similarity}.
Training consumes triples $(q_C, x^+, x^-)$ in which $x^+ \models C$ and $x^-$ is a hard negative.
The model predicts the \emph{margin}, $M$, and is regressed onto the \emph{label}, $y$,
\begin{equation}
\label{eq:app-obj}
    M(q_C, x^+, x^-) = \inner{f(q_C)}{f(x^+) - f(x^-)} ,
    \qquad
    y(q_C, x^-) = \sum_{i \in C} \ind{x^- \not\models c_i} ,
\end{equation}
under the squared loss $\mathcal{L}(q_C, x^+, x^-) = \big( M(q_C, x^+, x^-) - \red{y(q_C, x^-) / V} \big)^2$\red{, where $V > 0$ is a fixed scale}.
\end{definition}

\red{The scale $V$ is a hyperparameter of the trainer. Under the unit-norm constraint it cannot be absorbed into $f(q_C)$ by rescaling, so it appears explicitly in the bounds below.}

\begin{definition}[Linear recovery, after \citealp{garg2026many}]
\label{def:rec}
Let $S \subseteq L$ and $\epsilon > 0$.
Attribute $z_i$ is \emph{$(\epsilon, S)$-linearly recovered} by a probe $(b_i, \beta_i) \in \mathbb{R}^d \times \mathbb{R}$ if
\begin{equation}
\label{eq:app-rec}
    \big| \inner{b_i}{f(x)} + \beta_i - z_i(x) \big| < \epsilon
    \qquad \text{for all } x \in S .
\end{equation}
We write $r_i(x) = \inner{b_i}{f(x)} + \beta_i - z_i(x)$ for the \emph{residual}, so that Eq.~\eqref{eq:app-rec} reads $|r_i(x)| < \epsilon$ on $S$.
\citet{garg2026many} state linear accessibility with a strictly linear probe, $g_i(x) = \inner{b_i}{x}$; we allow the offset $\beta_i$, which is the usual linear probe with bias and is exactly linear recovery of $z_i$ from the homogeneous lift $(f(x), 1) \in \mathbb{R}^{d+1}$.
On the upper-bound side the offset is free, since affine probes contain linear ones (take $\beta_i = 0$) and existence results transfer verbatim.
On the lower-bound side, their Theorem 12 extends the dimension lower bound to binary-valued features, and its Corollary 13 shows that a bias and a monotonic activation do not raise the asymptotic capacity, so allowing $\beta_i$ does not let the model evade the bound; the binary restriction of our attributes is settled by the same theorem.
\end{definition}

Throughout, $S$ is the set of documents appearing in training triples, and we abbreviate
\begin{equation}
\label{eq:deltas}
    \Delta f = f(x^+) - f(x^-) ,
    \qquad
    \Delta z_i = z_i(x^+) - z_i(x^-) ,
    \qquad
    \Delta r_i = r_i(x^+) - r_i(x^-) .
\end{equation}

{\color{red}%
Let $U = \operatorname{span}\{ f(x) : x \in S \}$ be the span of the document embeddings.
We assume $U \neq \mathbb{R}^d$: the document embeddings occupy a proper subspace, as observed for trained encoders \textcolor{orange}{(cite)}.
Since every $f(x)$ lies in $U$, replacing a probe vector $b_i$ by its orthogonal projection onto $U$ leaves $\inner{b_i}{f(x)}$ unchanged for all $x \in S$ and so preserves Eq.~\eqref{eq:app-rec}; we therefore take $b_i \in U$ without loss of generality.
}

\subsection{Four lemmas}

The first lemma replaces the case split of Definition~\ref{def:attr} by a single arithmetic expression, which is what allows positive and negated conditions to be handled together.

\begin{lemma}[Polarity identity]
\label{lem:polarity}
For every $x \in L$ and every condition $c_i$,
\begin{equation}
\label{eq:app-polarity}
    \ind{x \not\models c_i} = \tfrac{1}{2}\big( 1 - s_i ( 2 z_i(x) - 1 ) \big) .
\end{equation}
\end{lemma}

\begin{proof}
Both sides take values in $\{0,1\}$, so it suffices to check the four combinations of $s_i$ and $z_i(x)$.
If $s_i = +1$ the right-hand side is $\tfrac{1}{2}(1 - 2z_i(x) + 1) = 1 - z_i(x)$, which is $0$ when $z_i(x) = 1$ and $1$ when $z_i(x) = 0$; by Definition~\ref{def:attr} a positive condition is satisfied exactly when $z_i(x) = 1$, so this agrees with the left-hand side in both cases.
If $s_i = -1$ the right-hand side is $\tfrac{1}{2}(1 + 2z_i(x) - 1) = z_i(x)$, which is $0$ when $z_i(x) = 0$ and $1$ when $z_i(x) = 1$; a negated condition is satisfied exactly when $z_i(x) = 0$, so again the two sides agree.
\end{proof}

\begin{lemma}[Attribute values of the positive]
\label{lem:pos}
If $x^+ \models C$, then $z_i(x^+) = \tfrac{1}{2}(1 + s_i)$ for every $i \in C$.
\end{lemma}

\begin{proof}
Fix $i \in C$.
Since $x^+ \models c_i$ we have $\ind{x^+ \not\models c_i} = 0$, so Lemma~\ref{lem:polarity} gives $\tfrac{1}{2}(1 - s_i(2 z_i(x^+) - 1)) = 0$, that is, $s_i (2 z_i(x^+) - 1) = 1$.
Multiplying both sides by $s_i$ and using $s_i^2 = 1$ yields $2 z_i(x^+) - 1 = s_i$, and solving for $z_i(x^+)$ gives the claim.
\end{proof}

% The value $\tfrac{1}{2}(1 + s_i)$ is $1$ for a positive condition and $0$ for a negated one, as it must be: the positive document has every attribute the query requires and none it forbids.

The next lemma is the pivot of the whole argument.
The label of Definition~\ref{def:obj} is a count computed from $x^-$ alone, yet the margin it is regressed onto is antisymmetric in $x^+$ and $x^-$.
Lemma~\ref{lem:label} shows the two are nonetheless compatible, by rewriting the count as a signed difference.

\begin{lemma}[Antisymmetric form of the label]
\label{lem:label}
If $x^+ \models C$, then
\begin{equation}
\label{eq:app-label}
    y(q_C, x^-) = \sum_{i \in C} s_i \, \Delta z_i .
\end{equation}
\end{lemma}

\begin{proof}
Fix $i \in C$.
Expanding Lemma~\ref{lem:polarity} at $x^-$ and collecting terms,
\begin{equation}
    \ind{x^- \not\models c_i}
    = \tfrac{1}{2}\big( 1 - s_i(2 z_i(x^-) - 1) \big)
    = \tfrac{1}{2}(1 + s_i) - s_i z_i(x^-) .
\end{equation}
By Lemma~\ref{lem:pos} the leading term is exactly $z_i(x^+)$, and multiplying it by $s_i^2 = 1$ lets us write it as $s_i z_i(x^+)$.
Hence
\begin{equation}
    \ind{x^- \not\models c_i} = s_i z_i(x^+) - s_i z_i(x^-) = s_i \, \Delta z_i .
\end{equation}
Summing over $i \in C$ and applying Definition~\ref{def:obj} gives Eq.~\eqref{eq:app-label}.
\end{proof}

\begin{remark}
\label{rem:constant}
The rewriting in Lemma~\ref{lem:label} is not cosmetic.
Written in terms of $x^-$ alone the label is $y = \tfrac{1}{2}\big(k + \sum_{i \in C} s_i\big) - \sum_{i \in C} s_i z_i(x^-)$, whose leading term is a nonzero constant.
A margin is antisymmetric, $M(q_C, x, x) = 0$, and so can never reproduce a constant; it is only because $x^+ \models C$ forces that constant to equal $\sum_{i \in C} s_i z_i(x^+)$ that the label takes a form a margin can match.
This is also why the objective of Eq.~\eqref{eq:hamming} is stated over triples rather than over single documents.
\end{remark}

\begin{lemma}[Margin decomposition]
\label{lem:decomp}
Let $b_i \in \mathbb{R}^d$ for $i \in C$ and set \red{$u = \sum_{i \in C} s_i b_i$}.
Then
\begin{equation}
\label{eq:app-decomp}
    \red{\inner{u}{\Delta f}} = \sum_{i \in C} s_i \Big( \inner{b_i}{f(x^+)} - \inner{b_i}{f(x^-)} \Big) .
\end{equation}
\end{lemma}

\begin{proof}
The inner product is bilinear, hence linear in its first argument: $\inner{\sum_{i \in C} s_i b_i}{\Delta f} = \sum_{i \in C} s_i \inner{b_i}{\Delta f}$.
It is also linear in its second argument, so $\inner{b_i}{\Delta f} = \inner{b_i}{f(x^+)} - \inner{b_i}{f(x^-)}$ by Eq.~\eqref{eq:deltas}.
Substituting the second identity into the first gives Eq.~\eqref{eq:app-decomp}.
\end{proof}

\subsection{Main results}

{\color{red}%
\begin{theorem}[Recovery implies low loss]
\label{thm:forward}
Let $S \subseteq L$ and $\epsilon > 0$, suppose every attribute $z_i$ with $i \in C$ is $(\epsilon, S)$-linearly recovered by a probe $(b_i, \beta_i)$ with $b_i \in U$, and let $u = \sum_{i \in C} s_i b_i$.
If $V \ge \|u\|$, then the unit-norm query representation
\begin{equation}
\label{eq:app-unitq}
    f(q_C) = \frac{1}{V} ( u + w ) ,
    \qquad
    w \perp U ,
    \quad
    \|w\|^2 = V^2 - \|u\|^2 ,
\end{equation}
satisfies
\begin{equation}
    \mathcal{L}(q_C, x^+, x^-) < \Big( \frac{2 k \epsilon}{V} \Big)^2
\end{equation}
for every triple with $x^+, x^- \in S$ and $x^+ \models C$.
\end{theorem}

\begin{proof}
A vector $w \perp U$ of the stated norm exists because $U \neq \mathbb{R}^d$, and $\|f(q_C)\|^2 = V^{-2} ( \|u\|^2 + \|w\|^2 ) = 1$ because $u \in U$ and $w \perp U$.
Since $\Delta f \in U$ we have $\inner{w}{\Delta f} = 0$, so $M = V^{-1} \inner{u}{\Delta f}$ and
\begin{equation}
    M - y / V = V^{-1} \big( \inner{u}{\Delta f} - y \big) .
\end{equation}
By Lemma~\ref{lem:decomp} the quantity $\inner{u}{\Delta f}$ decomposes over conditions, and by Lemma~\ref{lem:label} so does the label.
Subtracting them termwise,
\begin{equation}
    \inner{u}{\Delta f} - y
    = \sum_{i \in C} s_i \Big( \big( \inner{b_i}{f(x^+)} - \inner{b_i}{f(x^-)} \big) - \Delta z_i \Big) .
\end{equation}
Regrouping each summand by document rather than by quantity, and using $\Delta z_i = z_i(x^+) - z_i(x^-)$,
\begin{equation}
    \inner{u}{\Delta f} - y
    = \sum_{i \in C} s_i \Big( \big( \inner{b_i}{f(x^+)} - z_i(x^+) \big) - \big( \inner{b_i}{f(x^-)} - z_i(x^-) \big) \Big)
    = \sum_{i \in C} s_i \, \Delta r_i ,
\end{equation}
by Definition~\ref{def:rec}, the offsets $\beta_i$ cancelling because each appears once at $x^+$ and once at $x^-$.
The error of the objective is therefore exactly a signed sum of differences of probe residuals, divided by $V$.
Applying the triangle inequality to the sum, then $|s_i \Delta r_i| = |s_i| \, |\Delta r_i| = |\Delta r_i|$, and then the triangle inequality again inside each term,
\begin{equation}
    \big| \inner{u}{\Delta f} - y \big|
    \;\le\; \sum_{i \in C} \big| \Delta r_i \big|
    \;\le\; \sum_{i \in C} \Big( \big| r_i(x^+) \big| + \big| r_i(x^-) \big| \Big)
    \;<\; \sum_{i \in C} 2\epsilon
    \;=\; 2 k \epsilon ,
\end{equation}
where the strict inequality applies Eq.~\eqref{eq:app-rec} once at $x^+$ and once at $x^-$ for each of the $k$ conditions, both being members of $S$.
Squaring preserves the inequality for nonnegative quantities, giving $\mathcal{L} = V^{-2} \big( \inner{u}{\Delta f} - y \big)^2 < (2k\epsilon / V)^2$.
\end{proof}

If $U = \mathbb{R}^d$ the construction forces $w = 0$ and hence $V = \|u\|$: the scale is then fixed by the probes of each query rather than being a single free constant.
}

Combining Theorem~\ref{thm:forward} with the construction of \citet{garg2026many} bounds the dimension required to realize the objective.
Their theorem assumes linear representation, $f(x) = A z(x)$ with sparse attribute vectors, which is stronger than the linear accessibility used above; we adopt it as a hypothesis.
The corollary is therefore an existence result about representations, not a property of the trained encoder.

\begin{corollary}[Conditional capacity]
\label{cor:capacity}
Assume additionally that document embeddings are linearly represented, $f(x) = A z(x)$ with $A \in \mathbb{R}^{d \times m}$ drawn as in the random construction of \citet{garg2026many}, and that the attribute vector $z(x) \in \{0,1\}^m$ of every $x \in S$ has at most $k'$ nonzero entries.
Their upper bound is stated as $d = O_\epsilon(k'^2 \log m)$; the random construction proving it supplies, for any $\epsilon > 0$, probes attaining Eq.~\eqref{eq:app-rec} once $d = O(k'^2 \epsilon^{-2} \log m)$, the $\epsilon^{-2}$ entering through the incoherence $\mu \approx \epsilon / k'$ of the random directions.
Taking $\epsilon = \delta / 2k$ and applying Theorem~\ref{thm:forward}, margin loss below \red{$(\delta / V)^2$, i.e., error $\delta$ on the count scale,} on every $k$-condition query over these documents is realizable once
\begin{equation}
\label{eq:app-capacity}
    d = O\!\left( \frac{k'^2 k^2}{\delta^2} \, \log m \right) ,
    \qquad \text{equivalently} \qquad
    m = \exp\!\left( \Omega\!\left( \frac{d \, \delta^2}{k^2 k'^2} \right) \right) .
\end{equation}
\end{corollary}

The dependence on the number of attributes is logarithmic.
The polynomial factor arises because Theorem~\ref{thm:forward} requires per-probe error $\epsilon = \delta / 2k$, while the $d = O_\epsilon(k^2 \log m)$ form of \citet{garg2026many} holds at fixed $\epsilon$.
\red{The hypothesis $V \ge \|u\|$ of Theorem~\ref{thm:forward}, on the norm of the signed sum of the constructed probes, is taken as part of the assumption on the construction.}

The converse recovers probes from a trained model in two steps: differencing two queries isolates a single attribute, and a single positive document then fixes the offset.

\begin{theorem}[Low loss implies aggregate recovery]
\label{thm:back}
Let $\delta > 0$ and suppose $\mathcal{L}(q_C, x^+, x^-) < \delta^2$ for every triple with $x^+, x^- \in S$ and $x^+ \models C$.
Then
\begin{equation}
\label{eq:app-agg}
    \Big| \red{V} \inner{f(q_C)}{\Delta f} - \sum_{i \in C} s_i \, \Delta z_i \Big| < \red{V} \delta
\end{equation}
for every such triple.
\end{theorem}

\begin{proof}
Both $\mathcal{L}$ and $\delta^2$ are nonnegative, so $\mathcal{L} < \delta^2$ is equivalent to $|M - \red{y / V}| < \delta$\red{, and multiplying by $V > 0$ gives $|VM - y| < V\delta$}.
Substituting Eq.~\eqref{eq:app-obj} for $M$ and Lemma~\ref{lem:label} for $y$ gives Eq.~\eqref{eq:app-agg}.
\end{proof}

Theorem~\ref{thm:back} constrains only the aggregate $\sum_{i \in C} s_i \Delta z_i$: a model could err on one condition and compensate on another without violating it.
Isolating a single attribute requires comparing two queries that differ in exactly one condition, which is the content of the following corollary.

\begin{corollary}[Per-attribute recovery under deletion closure]
\label{cor:delete}
Assume in addition that the query family contains $q_{C \setminus \{i\}}$ for some $i \in C$, that it assigns the same polarity to every condition the two queries share, and that both queries satisfy the hypothesis of Theorem~\ref{thm:back}.
Fix any reference $x_0 \in S$ with $x_0 \models C$; at least one exists, since every triple contains a positive.
Then $z_i$ is $(\red{2V\delta}, S)$-linearly recovered by the probe
\begin{equation}
\label{eq:app-probe}
    b_i = s_i \red{V} \big( f(q_C) - f(q_{C \setminus \{i\}}) \big) ,
    \qquad
    \beta_i = z_i(x_0) - \inner{b_i}{f(x_0)} .
\end{equation}
\end{corollary}

\begin{proof}
Applying Eq.~\eqref{eq:app-agg} to $q_C$ and to $q_{C \setminus \{i\}}$ in turn,
\begin{equation}
    \Big| \red{V} \inner{f(q_C)}{\Delta f} - \sum_{j \in C} s_j \Delta z_j \Big| < \red{V} \delta ,
    \qquad
    \Big| \red{V} \inner{f(q_{C \setminus \{i\}})}{\Delta f} - \sum_{j \in C \setminus \{i\}} s_j \Delta z_j \Big| < \red{V} \delta .
\end{equation}
Because the two queries agree in polarity on their shared conditions, the two sums differ in exactly the single term $s_i \Delta z_i$.
Subtracting the second bound from the first and applying the triangle inequality, together with linearity of the inner product in its first argument,
\begin{equation}
    \Big| \inner{\red{V} \big( f(q_C) - f(q_{C \setminus \{i\}}) \big)}{\Delta f} - s_i \Delta z_i \Big| < \red{2V\delta} .
\end{equation}
Multiplying the quantity inside the absolute value by $s_i$ leaves the value unchanged, since $|s_i| = 1$, and $s_i^2 = 1$ reduces $s_i \cdot s_i \Delta z_i$ to $\Delta z_i$, giving
\begin{equation}
\label{eq:app-diff}
    \big| \inner{b_i}{\Delta f} - \Delta z_i \big| < \red{2V\delta}
    \qquad \text{for every triple.}
\end{equation}
It remains to fix the offset.
The hypothesis of Theorem~\ref{thm:back} ranges over every triple with $x^+, x^- \in S$ and $x^+ \models C$, so we may take $x^+ = x_0$ and let $x^- = x$ range over all of $S$.
For such a pair Eq.~\eqref{eq:app-diff} reads
\begin{equation}
    \big| \inner{b_i}{f(x_0)} - \inner{b_i}{f(x)} - z_i(x_0) + z_i(x) \big| < \red{2V\delta} .
\end{equation}
By the definition of $\beta_i$ in Eq.~\eqref{eq:app-probe} we have $\inner{b_i}{f(x_0)} - z_i(x_0) = -\beta_i$, so the quantity inside is $-\big( \inner{b_i}{f(x)} + \beta_i - z_i(x) \big)$, whose absolute value is unchanged by the sign.
Hence $\big| \inner{b_i}{f(x)} + \beta_i - z_i(x) \big| < \red{2V\delta}$ for all $x \in S$, which is Eq.~\eqref{eq:app-rec} with $\epsilon = \red{2V\delta}$.
\end{proof}

{\color{red}%
The scale $V$ enters the two directions oppositely.
Theorem~\ref{thm:forward} gives loss $(2k\epsilon / V)^2$, which falls as $V$ grows, while Corollary~\ref{cor:delete} recovers each attribute only to error $2V\delta$, which rises.
A larger $V$ makes the objective easier to satisfy and its optimum less informative about individual attributes, which is the trade-off behind selecting $V$ on validation data in our experiments.
}

{\color{red}% binary supervision
The graded label is what carries the per-attribute structure.
Replacing it with the binary relevance target $\ind{x^- \not\models C}$ under the same squared loss still recovers each query's conjunction, but no longer its individual attributes: the deletion pair that suffices in Corollary~\ref{cor:delete} does not suffice here.

\begin{proposition}[Binary supervision recovers conjunctions, not attributes]
\label{prop:binary}
Let $\mathcal{L}_{\mathrm{bin}}(q_C, x^+, x^-) = \big( M(q_C, x^+, x^-) - \ind{x^- \not\models C} / V \big)^2$.
\begin{enumerate}
    \item[(i)] If $\mathcal{L}_{\mathrm{bin}} < \delta^2$ for every triple with $x^+, x^- \in S$ and $x^+ \models C$, then the conjunction indicator $x \mapsto \ind{x \models C}$ is $(V\delta, S)$-linearly recovered, in the sense of Eq.~\eqref{eq:app-rec}, by the probe $\big( V f(q_C),\, 1 - V \inner{f(q_C)}{f(x_0)} \big)$ for any $x_0 \in S$ with $x_0 \models C$.
    \item[(ii)] For every $V \ge \sqrt{2}$ there exist $S$, conditions $C = \{c_1, c_2\}$ of positive polarity, and a unit-norm embedding with $d = 4$ attaining $\mathcal{L}_{\mathrm{bin}} = 0$ on every triple of $q_C$ and of $q_{C \setminus \{2\}}$, such that $z_2$ is not $(\epsilon, S)$-linearly recovered by any probe for any $\epsilon \le \tfrac{1}{2}$.
\end{enumerate}
\end{proposition}

\begin{proof}
(i) Fix $x \in S$ and apply the hypothesis to the triple $(q_C, x_0, x)$, writing its label as $\ind{x \not\models C} = 1 - \ind{x \models C}$ and multiplying by $V$:
\begin{equation}
    \big| V \inner{f(q_C)}{f(x_0) - f(x)} - 1 + \ind{x \models C} \big| < V\delta .
\end{equation}
Negating the quantity inside the absolute value gives $\big| \inner{V f(q_C)}{f(x)} + \big( 1 - V \inner{f(q_C)}{f(x_0)} \big) - \ind{x \models C} \big| < V\delta$, which is Eq.~\eqref{eq:app-rec} for the stated probe.

(ii) Let $S$ contain four documents realizing the four values of $(z_1, z_2)$, let $e_1, \dots, e_4$ be orthonormal in $\mathbb{R}^4$, set $a = 1/V \le 1/\sqrt{2}$, and define
\begin{equation}
    f(x) = a \, \ind{x \models C} \, e_1 + a \, \ind{x \models c_1} \, e_2 + \sqrt{1 - a^2 \big( \ind{x \models C} + \ind{x \models c_1} \big)} \; e_3 ,
    \qquad
    f(q_C) = e_1 ,
    \qquad
    f(q_{C \setminus \{2\}}) = e_2 .
\end{equation}
The square root is real because $a^2 \big( \ind{x \models C} + \ind{x \models c_1} \big) \le 2a^2 \le 1$, and every embedding is unit norm by construction; the document embeddings lie in $\operatorname{span}\{e_1, e_2, e_3\}$, so $U \neq \mathbb{R}^4$ as assumed throughout.
Since $x^+$ satisfies its query's conditions, the margin of any triple of $q_C$ is $a \big( \ind{x^+ \models C} - \ind{x^- \models C} \big) = \ind{x^- \not\models C} / V$, and likewise $\ind{x^- \not\models c_1} / V$ for $q_{C \setminus \{2\}}$: exactly the scaled binary labels, so $\mathcal{L}_{\mathrm{bin}} = 0$ on every triple.
The two documents with $z_1 = 0$ satisfy neither $C$ nor $c_1$ and so share the embedding $e_3$, yet differ in $z_2$; any probe $(b_2, \beta_2)$ assigns both the value $v = \inner{b_2}{e_3} + \beta_2$, and Eq.~\eqref{eq:app-rec} at the two documents requires $|v| < \epsilon$ and $|v - 1| < \epsilon$; then $1 \le |v| + |v - 1| < 2\epsilon$ forces $\epsilon > \tfrac{1}{2}$.
\end{proof}
}


% \subsection{Remarks}

% \begin{remark}[Identifiability]
% \label{rem:diff}
% Eq.~\eqref{eq:app-diff} constrains $b_i$ only through its action on the observed differences, so $b_i$ is determined only up to components orthogonal to their span.
% Corollary~\ref{cor:delete} requires only that some probe attain the bound, so nothing downstream depends on this.
% The offset $\beta_i$ is pinned by the single reference document, which the triple structure supplies for free.
% The hypotheses quantify over triples on which the loss is controlled; empirically, this is the training set.
% \end{remark}

% \begin{remark}[Cost of query complexity]
% \label{rem:cost}
% Theorem~\ref{thm:forward} loses a factor $2k$, so attaining loss $\delta^2$ on $k$-condition queries demands per-probe error $\epsilon < \delta / 2k$.
% Through Corollary~\ref{cor:capacity} this quantity is priced in dimension quadratically in the document sparsity and again in the query size, so the width requirement is driven by the sparsities rather than by the size of the corpus.
% \end{remark}

% \begin{remark}[Non-discriminating conditions]
% \label{rem:disc}
% Under the standing assumption $x^+ \models C$, a condition fails to discriminate on a triple precisely when the negative also satisfies it; then $\Delta z_i = 0$ and, by Lemma~\ref{lem:label}, the condition contributes nothing to the \emph{label}, which is why the recorded \texttt{query\_distance} counts only discriminating conditions.
% The \emph{error budget} is not reduced in the same way: in Theorem~\ref{thm:forward} the residual term $s_i \Delta r_i$ of a non-discriminating condition is bounded by $2\epsilon$ but need not vanish, so the $k$ in $(2k\epsilon)^2$ must be read as the full number of queried conditions $|C|$.
% Queried conditions that do not discriminate thus add residual noise to the margin without adding signal to the label, a formal counterpart of constraint dilution.
% \end{remark}

% \begin{remark}[Consistency with the generator]
% \label{rem:gen}
% Our queries draw positive conditions from the features unique to $x^+$ and from those common to both products, and negated conditions from the features unique to $x^-$ and from those applying to neither.
% Common and neither features are satisfied by both documents, so by Remark~\ref{rem:disc} they contribute nothing to the label, and $y$ reduces to the count of unique-positive plus unique-negative features, which is the \texttt{query\_distance} recorded by our preprocessing.
% \end{remark}

% \begin{remark}[Normalization and the label scale]
% \label{rem:scale}
% The deployed encoder $\ell_2$-normalizes both sides, so that $\inner{f(q)}{f(x)}$ is a cosine similarity and $|M| \le 2$, while the raw label can reach $k$.
% The label scale addresses this. The trainer's target is $y / V$, attainable only if $V \ge k_{\max} / 2$.
% In our pipeline hard-negative labels are capped by \texttt{max\_distance} $= 10$ and easy negatives are remapped to $15$, so $V = 25$ places every target in $[0, 0.6]$, inside $[-2, 2]$.
% Under normalization $V$ is not absorbable: a unit-norm query realizes the probe sum $B_C = \sum_{i \in C} s_i b_i$ only through its direction $B_C / \lVert B_C \rVert$, with $V$ standing in for $\lVert B_C \rVert$, and the margin error becomes $2 k \epsilon / \lVert B_C \rVert$ plus a scale-mismatch term $y \, \lvert V - \lVert B_C \rVert \rvert / ( V \lVert B_C \rVert )$, vanishing when $\lVert B_C \rVert = V$.
% \red{If the normalized statement is needed exactly, add the norm-matching assumption $\lVert B_C \rVert = V$ or carry the mismatch term through Theorem~\ref{thm:forward}.}
% \end{remark}

\paragraph{Glossary.}
\begin{description}\setlength{\itemsep}{0pt}
    \item[$L$] Input set, the queries and documents composed of sequences of tokens. Not to be
    confused with the loss $\mathcal{L}$.
    \item[$S$] The subset of $L$ on which recovery is required; here the documents appearing in
    training triples. Easy negatives are excluded.
    \item[$f$] The embedding $f : L \rightarrow \mathbb{R}^d$. The analysis imposes no norm
    constraint; the deployed encoder normalizes its outputs.
    \item[$d$] Embedding dimension.
    \item[$x$] A document. $x^+$ is a positive, satisfying every condition of the query; $x^-$ is a
    hard negative drawn from the same annotated product pair.
    \item[$z_i$] Attribute indicator $z_i : L \rightarrow \{0,1\}$, equal to $1$ when the text has
    attribute $i$. Depends only on the text.
    \item[$m$] Total number of attributes in the corpus.
    \item[$q_C$] Query with condition set $C$.
    \item[$c_i$] A single condition, naming attribute $i$ together with a polarity.
    \item[$C$] The set of conditions of a query.
    \item[$k$] Number of conditions, $|C|$. The label depends only on discriminating conditions,
    but error bounds use the full $|C|$.
    \item[$k'$] Maximum number of attributes present in a single document; document-side
    sparsity, used only in Corollary~\ref{cor:capacity}.
    \item[$s_i$] Polarity of condition $c_i$; $+1$ when the query requires attribute $i$ to be
    present, $-1$ when it requires it absent. Always $|s_i| = 1$.
    \item[$b_i$] Linear probe vector for attribute $i$. A property of the attribute, not of the
    query: one probe serves both polarities.
    \item[$\beta_i$] Scalar offset of the probe for attribute $i$, fixed by a reference document.
    Equivalent to one extra coordinate, see Definition~\ref{def:rec}.
    \item[$M$] The margin $\inner{f(q_C)}{f(x^+) - f(x^-)}$, the quantity the model predicts.
    \item[$y$] The label, the number of conditions the negative violates.
    \item[$\mathcal{L}$] The loss $(M - y)^2$ on a triple.
    \item[$\epsilon$] Probe error: the tolerance in the recovery condition Eq.~\eqref{eq:app-rec}.
    \item[$\delta$] Margin error: the square root of the loss achieved on every triple.
    \item[$V$] Fixed label scale applied by the trainer. Absorbable for the unconstrained
    objective, and set to $1$ in the analysis.
    \item[$\ind{\cdot}$] Indicator, $1$ when its argument holds and $0$ otherwise.
    \item[$\models$] Satisfaction: $x \models c_i$ when document $x$ meets condition $c_i$, and
    $x \models C$ when it meets all of them.
\end{description}

\endgroup


\bibliography{related-work}
\bibliographystyle{iclr2026_conference}

\appendix
\section{\red{Full Results}}
\label{app:full-results}

\begin{table}[h]
\centering
\input{figs/main_results_table.tex}
\caption{\red{Test-split recall of every loss and negative-selection group, mean of $n=3$ seeds with the lowest and highest seed in brackets. Bold is the best value in a column; underline is the best within a loss. Synthetic queries are the ones each model trained on; human queries are the human-written set, scored on the same model. Text is Recall@5 and image Recall@20.}}
\label{tab:main-results}
\end{table}

\begin{table}[h]
\centering
\input{figs/significance_table.tex}
\caption{\red{Paired permutation tests for the differences Figure~\ref{fig:text-vs-image} argues about: $A - B$ in recall at each modality's cutoff, each group the mean of $n=3$ seeds, on the same queries as Table~\ref{tab:main-results}. The null distribution flips the sign of each positive product's per-query differences (text test queries share products; image and human queries are one per product); $p$ is one-sided, that $A$ is better than $B$, over 10{,}000 Monte Carlo flips with the add-one correction. Bold is $p < 0.05$.}}
\label{tab:significance}
\end{table}

\section{Loss Functions}
\label{app:loss-functions}

InfoNCE \citep{oord2018representation} treats every document in a batch of $B$ triples as a candidate for every query and is defined as

\begin{equation}
    \label{eq:trad-infonce}
    \mathcal{L}(q_i) = - \log \frac{\exp\big( s \inner{f(q_i)}{f(x_i^+)} \big)}{\sum_{j=1}^{B} \exp\big( s \inner{f(q_i)}{f(x_j^+)} \big) + \exp\big( s \inner{f(q_i)}{f(x_j^-)} \big)} ,
\end{equation}

where $(q_i, x_i^+, x_i^-)$ is the $i$-th triple and the sum runs over the $2B$ positives and negatives of the batch.
The target is one-hot on $x_i^+$, so the loss constrains only the ordering of the candidates.

SigLIP \citep{zhai2023sigmoid} replaces the softmax over candidates with an independent sigmoid classifier for each (query, candidate) pair and is defined as

\begin{equation}
    \label{eq:trad-siglip}
    \mathcal{L} = - \frac{1}{B} \sum_{i=1}^{B} \sum_{x \in X} \log \frac{1}{1 + e^{-t_i(x) \left( s \inner{f(q_i)}{f(x)} + b \right)}} ,
\end{equation}

where $X$ is the set of $2B$ candidate documents in the batch, $t_i(x) = +1$ if $x = x_i^+$ and $-1$ otherwise, and $s$ and $b$ are a learnable scale and bias.
Each pair is fit to its own target, so the loss constrains the absolute score of every candidate rather than its rank.

% CoSENT \citep{huang2024cosent} consumes pairs $(q_i, x_i)$ with relevance labels $r_i$ and penalizes every two pairs whose similarities are ordered against their labels:
CoSENT \citep{huang2024cosent} consumes pairs $(q_i, x_i)$ with ordinal relevance labels $r_i$ and penalizes every two pairs whose similarities violate ordering:

\begin{equation}
    \label{eq:trad-cosent}
    \mathcal{L} = \log \Big( 1 + \sum_{r_i > r_j} \exp\big( s \big( \inner{f(q_j)}{f(x_j)} - \inner{f(q_i)}{f(x_i)} \big) \big) \Big) ,
\end{equation}

where the sum runs over all ordered pairs of pairs in the batch with $r_i > r_j$.
The labels enter only through their ordering, so with binary labels $r_i \in \{0, 1\}$ the loss requires every positive pair to outscore every negative pair by a margin set by $s$.



\section{List of Prompts}
\label{app:prompts}

Placeholders in braces are substituted at generation time.
Attribute counts appear resolved to their configured value of five per bucket.

\subsection{Product Attribute Extraction}
\label{prompt:text-features}

Applied to each (positive, hard negative) product pair drawn from ESCI, with both product
records serialised into the corresponding placeholders.

\begin{lstlisting}[style=promptstyle]
### Product A:

{positive_product}

### Product B:

{substitute_irrelevant_product}

List up to 5 features common to both products ("common_features"), up to 5 features unique to product A ("unique_features_a"), up to 5 features unique to product B ("unique_features_b"), and up to 5 features that do not apply to either product ("neither_features")). When generating common_features, ensure they are present in both products in some way. For example, if product A is "low fat" and product B in "low sugar", then "low calories" could be in common_features. If there are fewer than 5 common features, generate as many as possible. Do not assume anything about products not written in the description. When generating neither_features, ensure they are opposite or mutually exclusive with features in one or both of the products. For example, if product A is "red" and product B is "orange", then "blue" could be in neither_features. Must not generate negated features, such as "not red", "no wifi", "without bluetooth", or "doesn't have x".Dont use "not" or "without". Use your imagination and create diverse neither_features. All features should be objective and no more than 5 words. Return ONLY JSON: {'common_features': ['feature1', 'feature2', ...], 'unique_features_a': ['feature1', 'feature2', ...], 'unique_features_b': ['feature1', 'feature2', ...], 'neither_features': ['feature1', 'feature2', ...]}
\end{lstlisting}

\subsection{Visual Attribute Extraction}
\label{prompt:image-features}

Applied to each (positive, hard negative) garment pair drawn from DeepFashion In-Shop.
Both photographs are supplied to the model alongside the prompt, in the order named.

\begin{lstlisting}[style=promptstyle]
### Product A:

The first image is Product A. Metadata: {positive_product}

### Product B:

The second image is Product B. Metadata: {hard_negative_product}

List up to 5 visual features common to both products ("common_features"), up to 5 visual features unique to product A ("unique_features_a"), up to 5 visual features unique to product B ("unique_features_b"), and up to 5 visual features that do not apply to either product ("neither_features").

When generating common_features, ensure they are visible in both products in some way. If there are fewer than 5 common features, generate as many as possible. Do not assume anything not visible in the images or stated in metadata.

When generating neither_features, ensure they are opposite or mutually exclusive with visual features in one or both products. For example, if product A is red and product B is orange, then "blue" could be in neither_features. Must not generate negated features, such as "not red", "no sleeves", "without pattern", or "doesn't have x". Dont use "not" or "without". Use your imagination and create diverse neither_features. All features should be objective and no more than 5 words.

Return ONLY JSON: {"common_features": ["feature1", "feature2"], "unique_features_a": ["feature1", "feature2"], "unique_features_b": ["feature1", "feature2"], "neither_features": ["feature1", "feature2"]}
\end{lstlisting}

\subsection{Query Rephrasing}
\label{prompt:rephrase}

Applied to the templated query to produce the rewritten variant used in the robustness evaluation.

\begin{lstlisting}[style=promptstyle]
Original query: '{query}'

Task: Rephrase the query so it keeps the exact same meaning but sounds more natural and slightly varied.

Rules:
- Preserve intent exactly (positive stays positive, negative stays negative).
- You MUST change at least 50-80% of the features/phrases, and only when a natural synonym exists.
- Do NOT force rewording if it sounds unnatural.
- Do NOT change the product/item name unless a very natural alternative exists.
- Do NOT change measurement units or numeric values.
- Keep the query fluent and realistic.

Return ONLY a JSON object: {"rephrased_query": "..."}
\end{lstlisting}



\end{document}
